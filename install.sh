#!/bin/sh

# link .vim folder and .vimrc
# ln -s ./vim/.vim ~/.vim
# ln -s ./vim/.vimrc ~/.vimrc

# copy vim theme into .vim/plugs/.../vim-airline-themes once :PlugInstall has been executed

# link .config/nvim folder(s)
# ln -s ./nvim/nvim ~/.config/nvim

# link ~ (bash) folder(s)
# link ~ (X) folder(s)
